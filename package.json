{
  "name": "workcosmo-perform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx -y live-server . --port=8094",
    "format": "npx prettier --write \"**/*.js\" \"*.html\""
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "live-server": "^1.2.2"
  }
}
